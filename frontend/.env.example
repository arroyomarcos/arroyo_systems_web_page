# Example environment file for the frontend.
# Copy this to .env and fill in real values. NEVER commit .env to git.

# The public URL of the backend API (no trailing slash).
# Local dev:  http://localhost:8001
# Production: https://api.arroyo-systems.com  (or your Render/Railway URL)
REACT_APP_BACKEND_URL="https://api.arroyo-systems.com"
